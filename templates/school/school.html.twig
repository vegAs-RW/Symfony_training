{% extends 'base.html.twig' %}

{% block content %}
    <h1> Ecole : {{ school.name }} </h1>

    <p> {{ school.description }} </p>

    <h2> Liste des formations </h2>
    <ul>
    {% for training in school.trainings %}
        <li><a href=" {{ path('show_training' , { 'id' : training.id} ) }}">{{ training.name }} </a></li>
    {% endfor %}
    </ul>

{% endblock %}
